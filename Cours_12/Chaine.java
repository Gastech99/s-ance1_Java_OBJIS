public class Chaine {
    /**
     * String: imuable (Contenu non modifiable)
     *      > Concat() -> Plus rapide que '+'
     *      > length()
     *      > toUppercase(), toLowerCase(), trim(), replace(<old>, <new>)
     *      > charAt(<index>)
     */

    public static void main(String[] args){

        String s = "Gassama";
        System.out.println(s.charAt(0));
    }
}